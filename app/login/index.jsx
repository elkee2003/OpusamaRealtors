import { View, Text } from 'react-native'
import React from 'react'
import LoginCom from '../../components/LoginComponent'

const Signin = () => {
  return (
    <View style={{flex:1}}>
      <LoginCom/>
    </View>
  )
}

export default Signin;