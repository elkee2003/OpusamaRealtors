import { View, Text, Image, } from 'react-native'
import React from 'react'
import ShortAlertList from '../../../components/AlertComponent/ShortAlertList'

const Alert = () => {

  return (
    <View style={{flex:1}}>
        <ShortAlertList/>
    </View>
  )
}

export default Alert;