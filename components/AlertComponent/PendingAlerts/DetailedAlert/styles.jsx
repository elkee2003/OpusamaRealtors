import { StyleSheet, } from 'react-native'

const styles = StyleSheet.create({
    container:{
        flex:1,
        marginTop:40,
        marginHorizontal:10,
    },
    header:{
        fontSize:30,
        fontWeight:'bold',
        textAlign:'center'
    },
    subHeader:{
        fontSize:17,
        fontWeight:'bold',
    },
    details:{
        fontSize:16,
    },
})

export default styles;
